{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "A weather application with React frontend and Node.js backend",
  "main": "server/server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm start",
    "client": "cd client && npm run dev",
    "install-deps": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "keywords": ["weather", "react", "nodejs", "express"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
